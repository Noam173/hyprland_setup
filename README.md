#!/usr/bin/env bash

# main.sh and arch_setup.sh have a shabeng line (#!/usr/bin/env bash) and the neccesary permissions so it can be run like ./
# iso_installer.ps1 has also the permissions to run in bash and cmd/powershell
